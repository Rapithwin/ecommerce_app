import 'package:flutter/material.dart';

class FavoritesSuccess extends StatelessWidget {
  const FavoritesSuccess({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

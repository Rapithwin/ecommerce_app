import 'package:flutter/widgets.dart';

class ProfileFailure extends StatelessWidget {
  const ProfileFailure({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
